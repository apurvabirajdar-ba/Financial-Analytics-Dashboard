type Transaction = { id: number | string; amount: number; category: string; status: string; date: string; user_id?: string };
type Props = { data: Transaction[]; onSort: (field: string) => void; sortBy: string; sortOrder: string };

export const TransactionTable = ({ data, onSort, sortBy, sortOrder }: Props) => <div className="table-scroll"><table className="transaction-table"><thead><tr><th>User</th><th onClick={() => onSort('date')}>Date {sortBy === 'date' && (sortOrder === 'asc' ? '↑' : '↓')}</th><th onClick={() => onSort('amount')}>Amount {sortBy === 'amount' && (sortOrder === 'asc' ? '↑' : '↓')}</th><th>Category</th><th>Status</th></tr></thead><tbody>{data.map((tx) => <tr key={tx.id}><td><span className="user-avatar">{(tx.user_id || 'U').slice(-1)}</span>{tx.user_id || 'User'}</td><td>{new Date(tx.date).toLocaleDateString('en-IN', { day: '2-digit', month: 'short', year: 'numeric' })}</td><td className={tx.category === 'Revenue' ? 'amount-income' : 'amount-expense'}>{tx.category === 'Revenue' ? '+' : '−'}₹{Number(tx.amount).toLocaleString('en-IN', { minimumFractionDigits: 2 })}</td><td><span className={`category-badge ${tx.category === 'Revenue' ? 'revenue' : 'expense'}`}>{tx.category}</span></td><td><span className={`status-badge ${tx.status === 'Paid' ? 'paid' : 'pending'}`}>{tx.status}</span></td></tr>)}</tbody></table></div>;
