type Props = { selected: string[]; onChange: (columns: string[]) => void; onClose: () => void; onExport: () => void };
const columns = [{ key: 'date', label: 'Date' }, { key: 'amount', label: 'Amount' }, { key: 'category', label: 'Category' }, { key: 'status', label: 'Status' }, { key: 'user_id', label: 'User ID' }];

export const CSVExportModal = ({ selected, onChange, onClose, onExport }: Props) => <div className="modal-backdrop" role="presentation" onMouseDown={onClose}><section className="export-modal" role="dialog" aria-modal="true" aria-labelledby="export-title" onMouseDown={(event) => event.stopPropagation()}><button className="modal-close" onClick={onClose} aria-label="Close export settings">×</button><p className="eyebrow">CONFIGURE REPORT</p><h2 id="export-title">Export transactions</h2><p>Choose the fields to include in your CSV download.</p><div className="column-options">{columns.map((column) => <label key={column.key}><input type="checkbox" checked={selected.includes(column.key)} onChange={() => onChange(selected.includes(column.key) ? selected.filter((key) => key !== column.key) : [...selected, column.key])} /> <span>{column.label}</span></label>)}</div><button className="export-button modal-export" disabled={!selected.length} onClick={onExport}>Download CSV</button></section></div>;
